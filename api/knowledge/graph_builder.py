"""Graph builder placeholder — NetworkX KG -> graph.pkl"""


def build_graph_from_sources(source_dir: str, out_path: str) -> None:
    """Placeholder implementation."""
    with open(out_path, "wb") as f:
        f.write(b"")
